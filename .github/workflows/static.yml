# Simple workflow for deploying static content to GitHub Pages
name: Deploy to GitHub Pages
on:
  # Runs on pushes targeting the default branch
  push:
    branches: ['main']
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
# Sets the GITHUB_TOKEN permissions to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write
# Allow one concurrent deployment
concurrency:
  group: 'pages'
  cancel-in-progress: true
jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SUPABASE_API_KEY: ${{ secrets.SUPABASE_API_KEY }}
          SUPABASE_URL_LC_CHATBOT: ${{ secrets.SUPABASE_URL_LC_CHATBOT }}
      - name: Setup Pages
        uses: actions/configure-pages@v3
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          # Upload dist repository
          path: './dist'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
# name: Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - main  # Adjust branch name as needed

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v2

#       - name: Install dependencies
#         run: npm install  # Or use yarn if preferred

#       - name: Build Vite.js application
#         run: npm run build  # Or any other build command for your Vite.js app
#         env:
#           OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#           SUPABASE_API_KEY: ${{ secrets.SUPABASE_API_KEY }}
#           SUPABASE_URL_LC_CHATBOT: ${{ secrets.SUPABASE_URL_LC_CHATBOT }}
      
#       - name: Deploy to GitHub Pages
#         uses: actions/deploy-pages@v4
#         with:
#           github_token: ${{ env.GITHUB_TOKEN }}
#           publish_dir: ./dist 
# Adjust the directory if your build output is different
# Simple workflow for deploying static content to GitHub Pages
#
# jobs:
#   # Single deploy job since we're just deploying
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v3

#       - name: Setup Pages
#         uses: actions/configure-pages@v4

#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           # Upload entire repository
#           path: '.'

#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4

#       - name: Install node_modules
#         run: npm install

#       - name: Build project
#         run: npm run build
#         env:
#           OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#           SUPABASE_API_KEY: ${{ secrets.SUPABASE_API_KEY }}
#           SUPABASE_URL_LC_CHATBOT: ${{ secrets.SUPABASE_URL_LC_CHATBOT }}
      
